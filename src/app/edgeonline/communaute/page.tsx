export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function EdgeOnlineCommunautePage() {
  return (
    <div className="pb-10">
      <div className="text-[10px] font-semibold uppercase tracking-[0.28em] text-white/45">Communauté</div>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-white">Messages & communauté</h1>
      <p className="mt-3 max-w-2xl text-sm leading-relaxed text-white/50">
        Placeholder : discussions, entraide, coaching, retours sur livrables.
      </p>
    </div>
  );
}

