import { EdgeLabShell } from "@/components/edge-site/edge-lab-shell";

export default function ParcoursMarketingLayout({ children }: { children: React.ReactNode }) {
  return <EdgeLabShell>{children}</EdgeLabShell>;
}
