/** Routes courtes `/programmes/:slug` (domaine vitrine Jessica après redirect middleware). */
export { default, generateMetadata, generateStaticParams } from "@/app/jessica-contentin/programmes/[slug]/page";
